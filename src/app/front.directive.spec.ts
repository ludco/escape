import { FrontDirective } from './front.directive';

describe('FrontDirective', () => {
  it('should create an instance', () => {
    const directive = new FrontDirective();
    expect(directive).toBeTruthy();
  });
});
